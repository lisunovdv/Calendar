/**
 *! Created by admin on 07.09.2016.
 */

function TagModule(name) {
    this.closed = ['textarea', 'button', 'div'];
    this.unclosed = ['input', 'br', 'hr'];

    this.stringTagName = name;
    this.isUnclosed = function () {
        if (.indexOf("Apple");)
    }
}

